cd D:\Work\EduShop\backend
npm install

# Tạo [.env](http://_vscodecontentref_/2) dựa trên ../.env.example và điền giá trị thật

# Start

npm run dev # or npm start (check backend/package.json)

cd D:\Work\EduShop\frontend
npm install

# Tạo [.env](http://_vscodecontentref_/3) dựa trên ../.env.example (VITE_API_BASE_URL=...)

# Start dev server (Vite)

# nếu [package.json](http://_vscodecontentref_/4) có "dev": "vite"

npm run dev

# nếu muốn npm start nhưng missing start, thêm script start:"vite" hoặc dùng npm set-script start "vite"
